{
  "name": "svel-ai-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute flight-monitor-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute flight-monitor-db --local --file=./schema.sql",
    "test": "node --test test/"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
